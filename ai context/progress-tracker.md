# Progress Tracker

Update this file after every spec — status table, Recent Activity log, and Known Gaps as they change. This is the single source of truth for what's actually built vs. planned, same role as its counterparts in `../../bikelog_server/context/` and `../../bikelog_client(web)/context/`.

## Current Phase

**All 22 specs are complete** (this paragraph's per-spec narrative below predates specs 20/21/22 — see the Spec Implementation Status table and Recent Activity for those three; not rewritten here to avoid duplicating what's already current there). Specs 09–13 have been through an implementation-review pass (explicitly re-audited against the real backend source, 2026-07-22). The app has a clean foundation (01), shared-component library (02), auth screens (03, 04), dashboard/bike-list (05), bike hub (06), fuel logs (07), mileage (08), maintenance catalog settings (09), maintenance logs & reminders (10), spending summary (11), bike issues (12), bike accessories (13), a native date picker for every date field (14), an optional `price` field on bike accessories (15), trend charts on both Spending and Mileage (18), and — as of this spec — AI spending/mileage insight cards plus a bike-scoped AI chat screen (19). Feature parity with the already-shipped web client is achieved for all bike-scoped screens, including both halves of the v2 planning pass: web's own specs 13 (trend charts) and 14 (AI integration) now both have mobile counterparts. One real, live bug was found and fixed during the specs 09–13 review (spec 10's reminders/maintenance-type-name resolution — see Recent Activity); specs 09, 11, 12, 13 were re-verified and found already correct. Two rounds of real, on-device bug reports from the user (specs 16, 17) have since been addressed via source review — `SelectPickerField`'s blank-value bug required two attempts (spec 16's height/prefill-race patches, then spec 17's `Menu`-based rewrite after those didn't fully resolve it live) and Spending's Month/Year filters went from free-typed text to real chevron steppers (spec 17). Spec 18 introduced this app's first charting dependency (`react-native-gifted-charts` + `react-native-svg`), deliberately reversing the "no charting library" stance documented since spec 01. Spec 19 needed no new dependency — it reuses `react-native-paper`, the existing `Toast.show` error pattern, and this app's card/tab idioms throughout, matching the web client's own spec 14 in scope but built fresh against this app's conventions.

## Spec Implementation Status

| Spec | Status      | Notes                                                                                                                                                    |
| ---- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 01   | ✅ Complete | Project setup & cleanup — see `context/specs/01-project-setup-cleanup.md`.                                                                               |
| 02   | ✅ Complete | Shared components + Picker decision — see `context/specs/02-shared-components.md`.                                                                       |
| 03   | ✅ Complete | Login — see `context/specs/03-login.md`.                                                                                                                 |
| 04   | ✅ Complete | Register — see `context/specs/04-register.md`.                                                                                                           |
| 05   | ✅ Complete | Dashboard/bike list — see `context/specs/05-dashboard-bike-list.md`.                                                                                     |
| 06   | ✅ Complete | Bike hub — see `context/specs/06-bike-hub.md`.                                                                                                           |
| 07   | ✅ Complete | Fuel logs — see `context/specs/07-fuel-logs.md`.                                                                                                         |
| 08   | ✅ Complete | Mileage stats — see `context/specs/08-mileage.md`.                                                                                                       |
| 09   | ✅ Complete | Maintenance catalog settings — see `context/specs/09-maintenance-catalog-settings.md`.                                                                   |
| 10   | ✅ Complete | Maintenance logs & reminders — see `context/specs/10-maintenance-logs-reminders.md`.                                                                     |
| 11   | ✅ Complete | Spending summary — see `context/specs/11-spending-summary.md`.                                                                                           |
| 12   | ✅ Complete | Bike issues — see `context/specs/12-bike-issues.md`.                                                                                                     |
| 13   | ✅ Complete | Bike accessories — see `context/specs/13-bike-accessories.md`.                                                                                           |
| 14   | ✅ Complete | Native date picker for date fields — see `context/specs/14-date-picker.md`.                                                                              |
| 15   | ✅ Complete | Bike accessory optional price — see `context/specs/15-bike-accessory-price.md`.                                                                          |
| 16   | ✅ Complete | Fix swipe-Edit tap and SelectPickerField display bugs — see `context/specs/16-swipe-edit-and-picker-fixes.md`.                                           |
| 17   | ✅ Complete | Fix SelectPickerField blank-value bug (Menu rewrite) + real month/year steppers — see `context/specs/17-select-input-and-spending-date-filter-fixes.md`. |
| 18   | ✅ Complete | Spending/mileage trend charts — see `context/specs/18-spending-mileage-trend-charts.md`.                                                                 |
| 19   | ✅ Complete | AI integration (insight cards + bike chat) — see `context/specs/19-ai-integration.md`.                                                                   |
| 20   | ✅ Complete | Image uploads (receipt/service/product/issue-evidence photos) — see `context/specs/20-image-uploads.md`.                                                 |
| 21   | ✅ Complete | Full-screen image viewer for already-uploaded images — see `context/specs/21-image-viewer.md`.                                                            |
| 22   | ✅ Complete | Bike documents (papers/IDs, mixed image+PDF, expiry tracking) — see `context/specs/22-bike-documents.md`.                                                 |

## Completed (already implemented)

- **Spec 01 — Project setup & cleanup.** `utils/axiosInstance.ts`'s response interceptor now normalizes errors to `{statusCode, message, errors}` and rejects the promise (was silently `return error;`); request-interceptor token attach (`AsyncStorage` key `"token"`) and 401 → `router.replace("/auth")` logic kept as-is. `utils/api.ts`/`hooks/useApi.ts` had `apiPut`/`useUpdateData` removed (no PUT routes in `bikelog_server`); `useDeleteData` renamed to `useDelete` to match `architecture.md`'s naming decision. `utils/colors.ts` now exports one real `COLORS` theme (`primary/background/text/border/white/textLight/card/shadow` + `success/danger/warning`) instead of 4 unused expense-tracker alternates. `app.json`'s `android.package` fixed to `com.moniruzzaman.bikelog` (`name`/`slug` were already `BikeLog`). `README.md` replaced with a real BikeLog description. All expense-tracker domain routes/components/types and `create-expo-app` scaffold leftovers deleted. `expo lint` and `tsc --noEmit` both pass clean.
- **Spec 02 — Shared components.** `components/main/shared/` now has `StatusBadge.tsx` (+ `issueStatusColors`/`accessoryStatusColors`/`accessoryUrgencyColors` lookup tables), `ConfirmDelete.ts` (`confirmDelete()` helper), `EmptyState.tsx`, `SectionLoading.tsx`, `SelectPickerField.tsx` (wraps `@react-native-picker/picker`, already-installed dependency), and a barrel `index.ts`. `expo lint`/`tsc --noEmit` both pass clean.
- **Spec 03 — Login.** `app/auth.tsx` is now a one-line wrapper around the new `components/main/Auth/LoginForm.tsx`. Posts to `/auth/login`, reads the top-level `token` (confirmed via `dummy-data.md` + the Postman collection's test script that `data` is always `null` on this endpoint), decodes it client-side with `jwt-decode` into `{userId, userEmail}` (matches the real backend JWT payload — confirmed in `bikelog_server/src/app/modules/user/user.services.ts`), and calls `handleSetToken` + `handleSetUser`. Email-format + non-empty password validation, password-visibility toggle (`MaterialCommunityIcons` `eye`/`eye-off`), `KeyboardAwareScrollView`, loading state on the button + inputs. `expo lint`/`tsc --noEmit` both pass clean.
- **Spec 04 — Register.** `app/register.tsx` is now a one-line wrapper around the new `components/main/Auth/RegisterForm.tsx`. Posts trimmed `{name, email, password}` to `/auth/register`, client-side validates name/email-format/password≥6-chars (matching the backend's own rule), toasts + redirects to `/auth` on success. `expo lint`/`tsc --noEmit` both pass clean.
- **Spec 05 — Dashboard/bike list.** `app/(tabs)/_layout.tsx` rebuilt (2-tab bar: Dashboard + Settings, wrapped in `AuthGuard`) — closes the previously-open "tab vs. header icon" question. `app/(tabs)/index.tsx` renders `Dashboard.tsx` (`components/main/Dashboard/`): `GET /bikes` via `useFetchData`, pull-to-refresh, skeleton/empty states, maps to `BikeCard.tsx` (swipe-right=Edit/green, swipe-left=Delete/red via `ReanimatedSwipeable`, "only one row open" tracked via a shared ref). Create/edit both go through `components/main/Bike/BikeFormModal.tsx`. `app/(tabs)/settings.tsx` is a real placeholder (spec 09 builds the actual screen). New `types/bike.types.ts` (`TBike`, `TCreateBikePayload`, `TUpdateBikePayload`). `expo lint`/`tsc --noEmit` both pass clean.
- **Spec 06 — Bike hub.** `app/bikes/[bikeId]/index.tsx` renders `BikeDetailPage.tsx` (`components/main/Bike/`): fetches the single bike, info card, Edit (reuses spec 05's `BikeFormModal`)/Delete (→ redirect to `/`), 6 nav tiles to the not-yet-built bike-scoped screens. New `app/bikes/_layout.tsx` wraps the whole `bikes/` stack in `AuthGuard` — required per `architecture.md`'s System Boundaries section (this stack sits outside `(tabs)`, so spec 05's tab-layout guard doesn't cover it), a requirement neither spec's own Design table mentioned. `expo lint`/`tsc --noEmit` both pass clean.
- **Spec 07 — Fuel logs.** `app/bikes/[bikeId]/fuel-logs.tsx` renders `FuelLog.tsx` (`components/main/FuelLog/`): paginated list, pull-to-refresh, `FuelLogCard.tsx` (swipe-right=Edit/swipe-left=Delete via `ReanimatedSwipeable`, client-side `totalCost` display). `FuelLogFormModal.tsx` never sends `totalCost` (server-computed), handles the 409-if-mileage-record-closed case. New `types/fuel-log.types.ts`.
- **Spec 08 — Mileage.** `app/bikes/[bikeId]/mileage.tsx` renders `Mileage.tsx` (`components/main/Mileage/`): 4 pill-tab sub-views (History/Monthly/Yearly/Lifetime), each its own component, client-side average computation where the API doesn't return one, null-safe rendering for the `approximate` block. New `types/mileage.types.ts`.
- **Spec 09 — Maintenance catalog settings.** `app/(tabs)/settings.tsx` renders `SettingsCatalog.tsx` (`components/main/SettingsCatalog/`): two expandable sections (maintenance types, engine oil types), inline create forms, `.data` envelope drilling (no `{result, meta}` wrapper on either catalog endpoint — confirmed via `bikelog_server/src/app/modules/maintenanceType/`and`engineOilType/*.service.ts`, both plain `.find()` calls). New `types/catalog.types.ts`.
- **Spec 10 — Maintenance logs & reminders.** `app/bikes/[bikeId]/maintenance-logs.tsx` renders `MaintenanceLog.tsx` (`components/main/MaintenanceLog/`): list + `RemindersBanner.tsx` + `MaintenanceLogFormModal.tsx` (catalog pickers, `partsReplaced` comma-split, `nextDueOdometer` never sent). New `types/maintenance-log.types.ts`. **A real bug was found and fixed in the 2026-07-22 review pass** — see Recent Activity.
- **Spec 11 — Spending summary.** `app/bikes/[bikeId]/spending.tsx` renders `Spending.tsx` (`components/main/Spending/`): Month/Year/Lifetime pill tabs, `SpendingSummaryView.tsx` shared across all three, correctly uses the backend's real `cat.total` field (not `cat.amount`, which the spec's own sample used). New `types/spending.types.ts`.
- **Spec 12 — Bike issues.** `app/bikes/[bikeId]/issues.tsx` renders `BikeIssue.tsx` (`components/main/BikeIssue/`): paginated list with All/Open/Resolved filter, `BikeIssueCard.tsx` (status via `StatusBadge`, dedicated toggle button calling `PATCH .../status`, never generic `PATCH`), `BikeIssueFormModal.tsx` (title/description/dateReported only — no `notes` field, which doesn't exist on the backend). New `types/bike-issue.types.ts`.
- **Spec 13 — Bike accessories.** `app/bikes/[bikeId]/accessories.tsx` renders `BikeAccessory.tsx` (`components/main/BikeAccessory/`): paginated list with urgency+status filters (combinable), `BikeAccessoryCard.tsx` (two `StatusBadge`s), `BikeAccessoryFormModal.tsx` — first real usage of `SelectPickerField` (spec 02's Picker decision) for two 3-option enums. New `types/bike-accessory.types.ts`.
- **Spec 14 — Native date picker.** Installed `@react-native-community/datetimepicker@8.4.4` (`npx expo install`, config plugin auto-registered in `app.json`). New `components/main/shared/DatePickerField.tsx` (mirrors `SelectPickerField`'s prop shape; converts the app's `"yyyy-MM-dd"` string convention to/from a JS `Date` via `date-fns`), exported from the shared barrel. Replaced all 5 manually-typed date `TextInput`s across the app: `BikeFormModal.purchaseDate` (also removed the now-dead `DATE_REGEX` validation), `FuelLogFormModal.date`, `BikeIssueFormModal.dateReported`, `MaintenanceLogFormModal.serviceDate` + `nextDueDate`. No payload/type/backend changes — every form still submits the identical `"yyyy-MM-dd"` string as before.
- **Spec 15 — Bike accessory optional price.** Mirrors a backend change made to `bikeAccessory` (verified directly against `../bikelog_server` source — `price?: number`, optional, `.positive()` validation — since the backend hasn't been deployed yet). Added `price?: number` to `TBikeAccessory`/`TCreateBikeAccessoryPayload`/`TUpdateBikeAccessoryPayload`. `BikeAccessoryFormModal.tsx` gained a "Price (optional)" field (string state, `DECIMAL_REGEX` + `> 0` validation mirroring the backend's `.positive()`, omitted from the payload entirely when blank). `BikeAccessoryCard.tsx` shows `৳{price.toFixed(2)}` when set, nothing otherwise. `expo lint`/`tsc --noEmit` both pass clean. **Not exercised against a live backend** — see Known Gaps.
- **Spec 16 — Fix swipe-Edit tap and SelectPickerField display bugs.** Two real, user-reported on-device bugs. (1) Swapped the `TouchableOpacity` import from `"react-native"` to `"react-native-gesture-handler"` in all 5 swipeable cards (`BikeCard`, `FuelLogCard`, `MaintenanceLogCard`, `BikeIssueCard`, `BikeAccessoryCard`) — the standard fix for taps on a `Swipeable`'s revealed action buttons being swallowed by the gesture recognizer, since those buttons render outside the library's `GestureDetector`-wrapped content. Root cause of why Delete "worked" but Edit didn't remains a leading theory, not fully confirmed (see Known Gaps). (2) Fixed `SelectPickerField.tsx`'s selected-text truncation by raising its hardcoded picker height from `40` to `50` (Android's native picker row needs ~48–50dp+). (3) Fixed `MaintenanceLogFormModal.tsx`'s broken-prefill race by gating both catalog-backed pickers (maintenanceType, oilType) behind their options list being non-empty, showing `SectionLoading` until then — previously the picker was handed a `selectedValue` before its matching `Picker.Item` existed. `expo lint`/`tsc --noEmit` both pass clean. **Not visually confirmed on-device** — see Known Gaps.
- **Spec 17 — Fix SelectPickerField blank-value bug (Menu rewrite) + real month/year steppers.** User reported, from real on-device use, that spec 16's two fixes didn't fully resolve `SelectPickerField` showing blank after selecting a value (affecting both catalog-fetched usage in `MaintenanceLogFormModal.tsx` and static-options usage in `BikeAccessoryFormModal.tsx`), and that Spending's Month/Year tabs were still a plain free-typed text field rather than a real picker. (1) Rewrote `SelectPickerField.tsx` on top of `react-native-paper`'s `Menu`/`TouchableRipple` instead of a third narrow patch on `@react-native-picker/picker` — same prop interface, zero call-site changes in either form. Removed `@react-native-picker/picker` from `package.json` (now fully unused, confirmed via grep) and re-ran `yarn install` to sync `yarn.lock`. (2) Added new shared `components/main/shared/YearStepper.tsx` and `MonthStepper.tsx` (chevron + label, `date-fns`-based month math), extracted from `YearlyMileageTab.tsx`'s already-working inline stepper pattern. Applied to Spending's `MonthTab`/`YearTab` (the reported bug) and, as a recommended same-pass fix, to Mileage's `MonthlyMileageTab.tsx` (identical plain-`TextInput` bug found while investigating) and refactored `YearlyMileageTab.tsx` to use the new shared `YearStepper` instead of its own inline copy. Removed dead styles (`selector`/`input` in `Spending.tsx`; `selectorRow`/`selectorLabel`/`monthInputWrapper`/`input` in `MonthlyMileageTab.tsx`; `yearSelector`/`yearButton`/`yearText` in `YearlyMileageTab.tsx`) left over from the replaced controls. `expo lint`/`tsc --noEmit` both pass clean. **Not visually confirmed on-device** — see Known Gaps; this is the highest-priority item to check next given two prior `SelectPickerField` fixes already didn't fully resolve the bug on-device.
- **Spec 18 — Spending/mileage trend charts.** v2 scope, client for backend spec 15's already-shipped trend endpoints, mirroring the already-shipped web client's own spec 13. Installed `react-native-gifted-charts@1.4.77` + `react-native-svg@15.12.1` (pinned to Expo SDK 54's expected version, not the latest — see Recent Activity) — this app's first charting dependency, deliberately reversing the "no charting library" stance documented in `architecture.md`/`project-overview.md`/`ui-context.md` since spec 01 (all three updated in this same pass). Added `TMonthlySpending`/`TSpendingTrend` to `types/spending.types.ts` and `TMileageTrend` (reusing the existing `TMonthlySummary`) to `types/mileage.types.ts`, plus a shared `CHART_COLORS` array in `utils/colors.ts`. `Spending.tsx` gained an inline `TrendTab` (mirroring its existing inline `MonthTab`/`YearTab`/`LifetimeTab` pattern) rendering a `BarChart` of `totalSpending` per month plus a `PieChart` donut of the most recent month's `categoryBreakdown` — no second network request, both charts fed from the same `useFetchData<TSpendingTrend>` call. New separate file `components/main/Mileage/MileageTrendTab.tsx` (mirroring `MonthlyMileageTab.tsx`'s shape, since Mileage already splits every tab into its own file, unlike Spending) renders a single `BarChart` of `totalDistanceKm` per month. Both screens' `TABS`/tab-switch types gained one new entry each (`"trend"`/`"trends"`), reusing the existing pill-tab styling unchanged. `expo lint`/`tsc --noEmit` both pass clean. **Not visually confirmed on-device** — same standing gap as every other spec; chart rendering itself (bar heights, donut segments, label fit at phone width) is type/lint-verified only.
- **Spec 19 — AI integration (insight cards + bike chat).** v2 scope, client for backend spec 16's already-shipped AI endpoints, mirroring the already-shipped web client's own spec 14. No new dependency. Added `TSpendingInsight`/`TMileageInsight` to `spending.types.ts`/`mileage.types.ts` and new `types/ai-assistant.types.ts` (`TChatMessage`, `TBikeChatResponse`). New `AiSpendingInsightCard.tsx`/`AiMileageInsightCard.tsx` (own local card styles, `useFetchData` against the two GET endpoints, `"Thinking..."` loading text), each rendered above their screen's tab bar. New `components/main/AiAssistant/AiAssistant.tsx` chat screen: local `useState<TChatMessage[]>([])` conversation, defensively reset on `bikeId` change via a render-time `prevBikeId` comparison (not an effect), optimistic user-message append, `usePost()` to `/bikes/:bikeId/ai/chat` resending full history each call, `ActivityIndicator` "AI is thinking..." bubble while pending, `Toast.show` on failure with no fabricated reply. New route `app/bikes/[bikeId]/assistant.tsx` (4-line wrapper, confirmed covered by `app/bikes/_layout.tsx`'s existing `AuthGuard` — no nested layout to override it). `BikeDetailPage.tsx`'s `TILES` gained a 7th `"AI Assistant"` entry (`robot-outline` icon). `expo lint`/`tsc --noEmit` both pass clean; no corrections needed vs. the spec's own Design sample. **Not visually confirmed on-device** — same standing gap as every other spec; the actual chat round-trip against the live `bikelog_server` was exercised via `curl` during the prior planning session, not from this app's own UI.

- **Spec 21 — Full-screen image viewer.** New `components/main/shared/ImageViewerModal.tsx` (exported from the shared barrel): `Portal`+`Modal` from `react-native-paper` with a full-bleed black `contentContainerStyle`, `expo-image` at `contentFit="contain"`, close via a top-right `close` `IconButton`, tap-outside-to-close via a nested-`Pressable` structure (outer full-screen `Pressable` calling `onDismiss`, inner image-area `Pressable` swallowing its own taps), a paging `FlatList` (`horizontal`, `pagingEnabled`, `onMomentumScrollEnd`-tracked `currentIndex`) plus `chevron-left`/`chevron-right` `IconButton`s for multi-image nav, both chevrons disabled (not hidden) at the first/last image, no wraparound, no pinch-zoom. `MultiImagePickerField.tsx`'s thumbnails gained an `onPress` opening the viewer at the tapped index (self-contained `useState`, no card changes). `ImagePickerField.tsx`'s tile tap now opens the viewer when a value is set (was always the replace action sheet); "replace" moved to a new small pencil-icon badge next to the existing delete `X`; the placeholder (no-value) tile still opens the action sheet on tap. `expo lint`/`npx tsc --noEmit` both pass clean. **Not visually confirmed on-device** — same standing gap as every other spec in this project.
- **Spec 22 — Bike documents (papers/IDs, mixed image+PDF, expiry tracking).** Client for the backend's now-shipped `bikeDocument` module (`bikelog_server`'s own spec 19, live-verified via curl on that side). Follows `bikeIssue`'s (spec 12) two-step create-then-attach template minus its status field/toggle, since documents have no lifecycle state. Installed `expo-document-picker@14.0.8` — this app's first PDF-picking dependency; its config plugin only sets iCloud entitlements gated behind `ios.usesIcloudStorage` (unset in `app.json`), so no `app.json` plugin entry was needed, confirmed by reading the plugin's own source rather than assuming. New `types/document-file.types.ts` (`TDocumentFile`, `TPickedFile`) and `types/bike-document.types.ts` (`IBikeDocument`, `TCreateBikeDocumentPayload`, `TUpdateBikeDocumentPayload`, `TBikeDocumentsApiResponse` — the last one not in the spec's own Design section but added to match the real paginated-envelope shape every other list endpoint in this app uses, mirroring `TBikeIssuesApiResponse`). New shared `components/main/shared/MultiFilePickerField.tsx` (exported from the barrel): mixed image/PDF tile row, `resourceType === "image"` renders an `expo-image` thumbnail that opens the existing `ImageViewerModal` (filtered to just the image-type files so the viewer's paging stays image-only), `resourceType === "raw"` renders a bordered `file-document-outline` tile with a truncated `originalName` label that opens `Linking.openURL` on tap; a 4-option add action sheet (Take Photo / Choose Photo from Library / Choose PDF / Cancel) reuses `MultiImagePickerField`'s exact camera/library calls for the first two and adds `expo-document-picker`'s `getDocumentAsync({ type: ["application/pdf"], multiple: true, copyToCacheDirectory: true })` for the third; all three paths share one 10-file-per-request cap (server's own limit, not a stricter client invention) with an info toast when a picker action returns more than the remaining allowance. New `components/main/BikeDocument/` (`BikeDocument.tsx`, `BikeDocumentCard.tsx`, `BikeDocumentFormModal.tsx`) mirrors `BikeIssue`'s file layout exactly, swapping its status-filter row and status-toggle button for nothing (no status field to filter/toggle) and its `MultiImagePickerField` for the new `MultiFilePickerField`. `BikeDocumentCard.tsx`'s expiry badge (`getExpiryBadge`, inlined per `BikeIssueCard`'s own precedent of not extracting a shared constants file for a single consumer) branches on `differenceInCalendarDays`: no `expiryDate` → no badge, negative → red "Expired", 0–30 → amber "Expires in N days", beyond → a neutral `COLORS.border`-background pill with the formatted date. `BikeDocumentFormModal.tsx` reuses `DatePickerField` for `expiryDate` with **no** `minimumDate`/`maximumDate` bound (per the spec's own reasoning — expiry is future-leaning but backfilling an already-expired paper is legitimate) and, unlike `BikeIssueFormModal.dateReported`, defaults to blank rather than today's date, since most documents don't expire. New route `app/bikes/[bikeId]/documents.tsx` (identical 4-line wrapper shape to every sibling route, covered by the existing `app/bikes/_layout.tsx` `AuthGuard`, no nested layout to override it). `BikeDetailPage.tsx`'s `TILES` array gained an 8th entry (`"Documents"`, `file-document-outline`). `expo lint` and `npx tsc --noEmit` both pass clean; no corrections were needed against the spec's own Design sample beyond adding the missing list-response envelope type. **Not visually confirmed on-device** — same standing gap as every other spec in this project, with the same extra weight spec 20 called out for its own first-ever camera/photo-library permission: this is the app's first-ever document-picker permission, and the actual action-sheet UX, mixed-tile rendering, and PDF `Linking.openURL` hand-off are all unconfirmed.

## Recent Activity

- **2026-07-30 (spec 22 implemented)** — Executed `context/specs/22-bike-documents.md` per user request, client for the backend's `bikeDocument` module (`bikelog_server`'s own spec 19, confirmed `✅ Complete` and live-verified via curl on that side before starting). Followed `bikeIssue`'s (spec 12) two-step create-then-attach template as directed by the spec, minus its status field/toggle since documents have no lifecycle state. Installed `expo-document-picker@14.0.8` (`npx expo install`, clean, no version-mismatch warning) — this app's first PDF-picking dependency. Per the spec's own instruction to confirm rather than assume whether an `app.json` plugin entry was needed, read the installed package's own `plugin/src/withDocumentPickerIOS.ts` directly: it only sets iCloud entitlements, gated behind `ios.usesIcloudStorage` (which `app.json` doesn't set), so it's a no-op for this app and no plugin entry was added. New `types/document-file.types.ts` (`TDocumentFile`, `TPickedFile`, both exactly as specified) and `types/bike-document.types.ts` (`IBikeDocument`, `TCreateBikeDocumentPayload`, `TUpdateBikeDocumentPayload` as specified, plus one addition beyond the spec's own Design section — `TBikeDocumentsApiResponse` — needed because the spec's pseudo-code read the list response as a bare `{result, meta}` while every real list endpoint in this app, confirmed via `BikeIssue.tsx`'s own precedent, actually arrives under a `{data: {result, meta}}` envelope). New `components/main/shared/MultiFilePickerField.tsx` (exported from the shared barrel): mixed image/PDF tile row branching on `resourceType` exactly as designed — image tiles reuse the existing `ImageViewerModal` (fed only the image-type files from the array, so the viewer's own paging stays image-only and never tries to render a PDF), raw tiles show a bordered `file-document-outline` icon plus a truncated `originalName` label and open `Linking.openURL` on tap; the add action sheet's first two options reuse `MultiImagePickerField`'s exact camera/library calls, the third calls `expo-document-picker`'s `getDocumentAsync({ type: ["application/pdf"], multiple: true, copyToCacheDirectory: true })` (confirmed against the installed version's own `.d.ts` that `type` accepts a string array and the result shape is `{canceled, assets}`, not the deprecated `{type: 'cancel'}` shape some of its own older docstrings still mention); all three paths share the server's real 10-file-per-request cap with a `Toast.show({type: "info", ...})` when a picker action returns more than the remaining allowance, per the spec's own explicit UX-parity instruction with the web client's equivalent spec. New `components/main/BikeDocument/` (`BikeDocument.tsx`, `BikeDocumentCard.tsx`, `BikeDocumentFormModal.tsx`) mirrors `BikeIssue`'s file-and-prop shape almost exactly, with its status-filter row and status-toggle button simply omitted rather than stubbed. `BikeDocumentCard.tsx`'s expiry badge (`getExpiryBadge`) is inlined per the spec's own instruction to follow `BikeIssueCard`'s precedent of not extracting a shared constants file for a single consumer — branches on `differenceInCalendarDays` into no-badge/red-"Expired"/amber-"Expires in N days"/neutral-formatted-date exactly as specified. `BikeDocumentFormModal.tsx`'s `expiryDate` field reuses `DatePickerField` with neither `minimumDate` nor `maximumDate` set (the spec's own explicit reasoning: expiry is future-leaning but backfilling an already-expired paper is legitimate) and defaults to blank rather than today, unlike `BikeIssueFormModal.dateReported`'s today-default. New route `app/bikes/[bikeId]/documents.tsx` (identical 4-line wrapper shape to every sibling route; explicitly confirmed covered by `app/bikes/_layout.tsx`'s existing `AuthGuard`, no nested layout to bypass it). `BikeDetailPage.tsx`'s `TILES` array gained an 8th entry (`"Documents"`, `file-document-outline`). `expo lint` and `npx tsc --noEmit` both pass clean, no new warnings in any created/modified file. Checked off all 11 Implementation-checklist items and all 11 Verify-checklist items in the spec file itself (each annotated with what was actually verified — code review against the confirmed backend contract and the installed `expo-document-picker` package's real type declarations, not a live device tap-through), and flipped the spec's own Status line from "⛔ Not started" to "✅ Complete". **Not visually confirmed on-device** — same standing gap as every other spec in this project, carrying the same extra weight spec 20 flagged for its own first-ever camera/photo-library permission: this is the app's first-ever document-picker permission, and the action-sheet UX, mixed image/PDF tile rendering, and the PDF `Linking.openURL` hand-off to the OS are all unconfirmed.

- **2026-07-28 (spec 21 implemented)** — Executed `context/specs/21-image-viewer.md` per user request. The spec was written the opposite way round from every other spec in this project — as a pre-implementation plan with 4 unresolved "Open Questions" awaiting review — so before writing any code the 4 decisions were settled (as directed, not asked again): **(1)** tap-target conflict on `ImagePickerField` (its tile tap was already claimed by the replace-image action sheet) → the spec's own **Option A** — with a value set, tapping the tile now opens the viewer, "replace" moved to a new small pencil (`MaterialCommunityIcons` `"pencil"`) badge next to the existing delete `X` badge, same small-badge visual treatment; the placeholder (no-value) state is unaffected, tap still opens the action sheet since there's nothing to view. **(2)** multi-image navigation → the spec's own recommended horizontally-paging `FlatList` (`pagingEnabled`, `onMomentumScrollEnd` computing `currentIndex` from scroll offset ÷ screen width) plus explicit `chevron-left`/`chevron-right` `IconButton`s calling `scrollToIndex`, giving both swipe and tap-to-navigate. **(3)** pinch-to-zoom → left out of scope for this pass, `expo-image`'s `contentFit="contain"` only, no gesture-based zoom/pan. **(4)** wrap-around → disabled (not hidden) at the bounds; checked `YearStepper.tsx`/`MonthStepper.tsx` for this project's existing disabled-icon-button-at-boundary convention first, per the task's own instruction, and found neither steps with any bound at all (both wrap unbounded via plain `+1`/`-1` arithmetic), so there was no existing convention to mirror — used `react-native-paper`'s `IconButton`'s own `disabled` prop plus a dimmed icon color as a fresh, self-contained convention for this one component. New `components/main/shared/ImageViewerModal.tsx` (`{visible, images, initialIndex, onDismiss}` props, `Portal`+`Modal` full-bleed black background, nested-`Pressable` tap-outside-to-close structure, counter overlay `"N / total"` shown only when `images.length > 1`), exported from `components/main/shared/index.ts`. `MultiImagePickerField.tsx`'s thumbnails gained `onPress={() => setViewerIndex(index)}` (new local `useState<number | null>`), no changes to the existing delete-badge or add-tile handlers. `ImagePickerField.tsx`'s `handlePress` now branches on `value` truthiness (view vs. action-sheet); its existing delete `X` badge's handler/position (`top: -6, right: -6`) is unchanged, and the new pencil badge sits immediately to its left (`right: 16`) using `COLORS.primary`. Confirmed, by reading all 4 card call sites directly (`FuelLogCard.tsx`, `MaintenanceLogCard.tsx`, `BikeAccessoryCard.tsx`, `BikeIssueCard.tsx`), that none pass any viewer-related prop and only use the pre-existing prop shapes — no card file needed any change, matching the spec's own "Where the viewer is triggered from" design. `expo lint` and `npx tsc --noEmit` both pass clean, no new warnings in any created/modified file. **No device/simulator available in this environment** — every behavior above (viewer open/close, swipe paging, chevron nav and its disabled-at-bounds styling, badge hit targets at the existing 64×64 tile size) is code-reviewed against `react-native-paper`'s and `expo-image`'s actual API surface, not visually confirmed — same standing limitation as every other spec in this project. Updated `context/specs/21-image-viewer.md`'s own Status line to `✅ Complete`, rewrote its "Note on format" blockquote to record the 4 decisions above as the historical record (it previously said "nothing built yet"), ticked all 6 Implementation checklist items, and filled in a real Verify checklist mirroring spec 20's style/phrasing convention. **Post-implementation correction (same session, before sign-off):** the shipped "tap outside to close" mechanism didn't actually work as designed — the image area's wrapping `Pressable` (added specifically to no-op image taps so they wouldn't bubble to the backdrop's `onDismiss`) was `flex: 1`, the same size as the backdrop itself, so it silently swallowed *every* tap in the modal, leaving the `X` button as the only working way to close it. Fixed by changing that wrapper from a `Pressable` to a plain `View` with `pointerEvents="box-none"`, so untouched taps now fall through to the outer backdrop `Pressable`. Net effect: a tap directly on the photo now also closes the viewer, not just taps on the surrounding black space — accepted as correct rather than reworked further, since precisely distinguishing "on the letterboxed pixels" from "on the surrounding area" isn't reliable without measuring the image's actual rendered box, and matches common mobile photo-viewer UX anyway. `expo lint`/`tsc --noEmit` re-run clean after the fix. Spec 21's Verify item and Design section were both updated to describe the shipped mechanism, not the originally-planned one.

- **2026-07-27 (spec 20 implemented)** — Executed `context/specs/20-image-uploads.md` per user request. Client for the backend's already-shipped Cloudinary image-upload feature (`bikelog_server`'s own spec 17), mirroring the already-shipped web client's own spec 15 but built fresh against this app's conventions (plain `useState`, no react-hook-form, `Alert.alert` action sheets, `Toast.show` errors). Installed `expo-image-picker@17.0.11` via `npx expo install` (SDK-54-compatible version resolved automatically, no mismatch warning) and added the `expo-image-picker` config-plugin entry to `app.json` with photos/camera permission strings (neither existed before — this is the app's first camera/photo-library permission of any kind). Closed the same real gap the web client's spec 15 closed on its side: added `apiPut`/`usePut` to `utils/api.ts`/`hooks/useApi.ts` (mirrors `apiPatch`/`usePatch`'s exact shape), since 3 of the 4 new backend routes are `PUT .../image` and this app had never needed a PUT verb before. New `types/image.types.ts` (`TCloudinaryImage`) and two new shared components exported from the `components/main/shared` barrel: `ImagePickerField.tsx` (single-image tile — tap opens a "Take Photo / Choose from Library / Cancel" `Alert.alert`, `expo-image`'s `<Image>` renders the thumbnail — finally giving that previously-unused dependency a real use — with an `X`-overlay delete and a spinner overlay while pending) and `MultiImagePickerField.tsx` (bikeIssue's variant — thumbnail row + a dashed "+" add tile, hidden once 5 images are reached, `selectionLimit` capping the library multi-select to the server's own per-request limit). Verified directly against the installed `expo-image-picker@17.0.11` package's own type declarations that `mediaTypes` accepts the modern `['images']` array form rather than the spec's originally-sketched (now-deprecated) `MediaTypeOptions.Images` enum — used the non-deprecated form instead, a small correction from the spec's literal sample, not a scope change. All four domains' type files gained their optional image field (`receiptImage`/`serviceImage`/`productImage`/`images`), matching the backend's additive/no-migration schema change. Every card (`FuelLogCard`, `MaintenanceLogCard`, `BikeAccessoryCard`, `BikeIssueCard`) owns its own `usePut`/`usePost`/`useDelete` instance directly, keyed off the `bikeId` prop it already receives (simpler here than on the web client, where the cards didn't already receive a `bikeId` prop and had to fall back to the record's own `.bike` field instead). Corrected `ai context/architecture.md`'s stale "`bikelog_server` has no PUT routes" line (spec 17 added real ones; noted in place rather than deleted, since the general point about `useUpdateData` being adapted-not-ported still stands) — per the spec's own step 16. `expo lint` and `npx tsc --noEmit` both pass clean, no warnings in any new/modified file. No physical device/simulator available in this environment, so the actual tap-through (action sheet, permission prompts, thumbnail rendering, live upload against Cloudinary) is code-reviewed against the confirmed spec 17 contract and the installed `expo-image-picker` package's real API surface, not literally exercised — same standing limitation as every prior spec in this project.

- **2026-07-27 (Mileage/Spending tab bar overflow fix)** — User-reported, real on-device bug: on `Mileage.tsx`, only History/Monthly/Yearly/Lifetime were visible, "taking the full width altogether" — spec 18's "Trends" tab (the 5th) was never seen. Root cause: both `Mileage.tsx`'s and `Spending.tsx`'s pill-tab-bar row was a plain `View` with `flexDirection: "row"`, no `flexWrap`/scroll — Mileage's 5 tabs overflow a phone-width screen (Spending's 4 currently still fit, but shares the identical code shape, so it was a latent instance of the same bug, not yet triggered). Fixed by wrapping both tab-bar rows in a horizontal `ScrollView` (`showsHorizontalScrollIndicator={false}`, `contentContainerStyle={styles.tabBar}`), so overflowing tabs are reachable by a horizontal swipe instead of being silently pushed off-screen. No change to either `TABS` array, tab pill styling, or tab-content rendering. `expo lint` and `npx tsc --noEmit` both pass clean. **Update, same day, second report**: user confirmed scrolling now works and "Trends" is reachable, but reported a new regression — the tab bar now "takes a large height which looks distorted." Root cause: the `ScrollView` was given only `contentContainerStyle`, no `style` of its own — a horizontal `ScrollView` as a flex child inside a column-flex parent needs its own cross-axis size bounded, or it doesn't reliably size itself to its content's natural height the way the old plain `View` did (a known RN gotcha; the standard fix is `flexGrow: 0` on the `ScrollView`'s own `style`, kept separate from `contentContainerStyle`). Fixed by adding a `tabBarScroll: { flexGrow: 0 }` style applied via `style={styles.tabBarScroll}` on both `ScrollView`s, plus `alignItems: "center"` added to the existing `tabBar` style (used as `contentContainerStyle`) so the pills are explicitly centered rather than relying on default cross-axis `stretch`. `expo lint`/`npx tsc --noEmit` both pass clean. **Still not re-confirmed on-device** — ask the user to confirm the tab bar now renders at a normal pill height with working horizontal scroll on both Mileage and Spending.
- **2026-07-26 (AI Assistant keyboard-avoidance fix)** — User-reported, real on-device bug: on `AiAssistant.tsx`, tapping the message `TextInput` brought up the keyboard but the input row (text field + send button) stayed hidden behind it — the screen had no keyboard-avoidance handling at all, unlike every form screen elsewhere in this app. Root cause: `AiAssistant.tsx`'s root layout was a plain `View`/`ScrollView`, with the input row as an ordinary flex child at the bottom — nothing repositioned it when the keyboard opened. Fixed by wrapping the input row in `KeyboardStickyView` from `react-native-keyboard-controller` (already a dependency, `KeyboardProvider` already mounted at the app root in `app/_layout.tsx` for the `KeyboardAwareScrollView` used elsewhere) — purpose-built for exactly this "chat input bar pinned above the keyboard" case, confirmed via its own source (`Animated.View` that `translateY`s by the live keyboard height, meant to be used as a normal flex child, not absolutely positioned). This is a different fix shape than the rest of the app's `KeyboardAwareScrollView` convention, deliberately — that component is for a single scrollable form where the whole screen should scroll to keep the focused field visible, but `AiAssistant.tsx`'s message list needs to stay independently scrollable while only the input bar moves, which is what `KeyboardStickyView` is for. Also added `backgroundColor: COLORS.background` to `styles.inputRow` (previously transparent), since the input row can now visually slide up over the message list while the keyboard is open and needs an opaque backing. `expo lint`/`npx tsc --noEmit` both pass clean. Not yet re-confirmed by the user after this fix — first real on-device signal for spec 19 overall (see Known Gaps).
- **2026-07-26 (spec 19 implemented)** — Executed `context/specs/19-ai-integration.md` per user request, the second and final half of the v2 planning pass that also produced spec 18 (trend charts, implemented first per the user's own earlier instruction). Client for backend spec 16's already-shipped AI endpoints (`GET .../ai/spending-insight`, `GET .../ai/mileage-insight`, `POST .../ai/chat`), all three re-confirmed live via `curl` during the prior planning session rather than re-verified this session, since no backend work was needed and the contract hadn't changed. No new dependency — built entirely from `react-native-paper` primitives (`TextInput`, `IconButton`, `ActivityIndicator`), the existing `Toast.show` error pattern, and this app's established card idiom, matching the web client's own spec 14 in scope. Added `TSpendingInsight` to `types/spending.types.ts`, `TMileageInsight` to `types/mileage.types.ts`, and a new `types/ai-assistant.types.ts` (`TChatMessage`, `TBikeChatResponse`) — the flat `types/<domain>.types.ts` layout this app already uses everywhere, not the web app's per-component `type/` subfolder convention. `AiSpendingInsightCard.tsx`/`AiMileageInsightCard.tsx` are near-identical files (own local `StyleSheet`, not shared with `Spending.tsx`/`Mileage.tsx`), each a plain `useFetchData` GET with `"Thinking..."` while loading, rendered as the first child above each screen's existing `tabBar`. `components/main/AiAssistant/AiAssistant.tsx` is the chat screen: conversation lives in local `useState<TChatMessage[]>([])`, reset defensively on `bikeId` change via a render-time `prevBikeId` comparison (`if (bikeId !== prevBikeId) { setPrevBikeId(bikeId); setMessages([]); }`) rather than a `useEffect` — same technique the web client's own spec 14 used to sidestep React's `set-state-in-effect` lint concern, ported here as cheap insurance even though RN's stack navigation likely already gives a fresh mount per bike (unconfirmed either way, no device available). Send flow optimistically appends the user's message before calling `usePost()` against `/bikes/:bikeId/ai/chat` with the full resent history, shows an `ActivityIndicator` + "AI is thinking..." bubble while pending, and on failure calls `Toast.show` without ever appending a fabricated assistant reply — the optimistic user message is left as the only visible change. New route `app/bikes/[bikeId]/assistant.tsx` (identical 4-line wrapper shape to every sibling route). Explicitly verified (spec's own step 6, not assumed) that `app/bikes/_layout.tsx`'s `AuthGuard`-wrapped `<Slot />` covers the new route — listed the actual `app/bikes/` file tree and confirmed no nested `_layout.tsx` exists under `[bikeId]/` that could bypass the parent guard. `BikeDetailPage.tsx`'s `TILES` array gained a 7th entry (`"AI Assistant"`, `robot-outline` — confirmed present in the installed `MaterialCommunityIcons` glyph map back during the planning session, not re-checked this session since nothing about the icon set changed). `expo lint` and `npx tsc --noEmit` both pass clean; `catch (error: any)` in the chat send handler matches this app's existing convention (already used in `BikeIssueFormModal.tsx` and every other form's catch block), confirmed allowed by this app's `eslint` config via the clean lint run. No corrections were needed against the spec's own Design sample code — this is the first spec in the 18/19 pair (and one of few in this project generally) where the implementation matched the written Design almost verbatim, the two insight cards' local styles being the only thing the spec left for the implementation to fill in. **Not visually confirmed on-device** — same standing gap as every other spec in this project (see Known Gaps).
- **2026-07-26 (spec 18 implemented)** — Executed `context/specs/18-spending-mileage-trend-charts.md` per user request, after the user reviewed a v2 planning pass (specs 18 + 19, written together) and authorized starting with spec 18 specifically; spec 19 (AI integration) is deliberately left unimplemented for now. Client for backend spec 15's already-shipped trend endpoints (`GET .../spending-summary/trend?months=3`, `GET .../mileage/trend?months=3`), mirroring the already-shipped web client's own spec 13 (Recharts) but built fresh against this app's own conventions rather than ported. Installed `react-native-gifted-charts` + `react-native-svg` — this app's first-ever charting dependency, deliberately reversing the "no charting library" stance documented in `architecture.md` (Invariant 6), `project-overview.md` (Out of Scope), and `ui-context.md` (Conventions) since spec 01; all three updated in this same pass rather than left stale, the same way spec 14 documented `@react-native-community/datetimepicker` as a new native dependency in place. **One real deviation from the spec's own Implementation step 1**: `yarn add react-native-gifted-charts react-native-svg` installed `react-native-svg@^15.15.5` (latest), but `npx expo install --check` flagged that Expo SDK 54 expects `15.12.1` for this native module — re-ran `npx expo install react-native-svg` to pin the SDK-compatible version rather than leaving a newly-introduced version mismatch in place (a real risk for a native module specifically, unlike a pure-JS package). `expo install --check` also flagged 6 pre-existing outdated packages (`expo`, `expo-font`, `expo-linking`, `expo-router`, `expo-web-browser`, `react-native-gesture-handler`) unrelated to this spec's own changes — left untouched, out of scope, not something spec 18 introduced. Added `TMonthlySpending`/`TSpendingTrend` to `types/spending.types.ts`, `TMileageTrend` (reusing the existing `TMonthlySummary`) to `types/mileage.types.ts`, and a shared `CHART_COLORS` array to `utils/colors.ts`. `Spending.tsx` gained an inline `TrendTab` function (matching how `MonthTab`/`YearTab`/`LifetimeTab` are already inline in that same file) rendering a `BarChart` of `totalSpending` per month plus a `PieChart` donut of the most recent month's `categoryBreakdown` — both fed from the single `useFetchData<TSpendingTrend>` call, no second request. New separate file `components/main/Mileage/MileageTrendTab.tsx` (mirroring `MonthlyMileageTab.tsx`'s exact shape, since Mileage already splits every tab into its own file — a deliberate difference from Spending's inline pattern, not an inconsistency) renders a single `BarChart` of `totalDistanceKm` per month, no donut on that side, matching the web app's own trend-chart scope split. Both screens' tab-key types/`TABS` arrays/render switches gained exactly one new entry each (`"trend"`/`"trends"`), reusing the existing pill-tab styling (`styles.tab`/`styles.tabActive`) completely unchanged — no new tab-bar styling introduced. `expo lint` and `npx tsc --noEmit` both pass clean; the `BarChart`/`PieChart` prop usage in the spec's own Design sample (`data`/`barWidth`/`spacing`/`roundedTop`/`yAxisThickness`/`xAxisThickness`/`donut`/`radius`/`innerRadius`) type-checked against the installed library with no corrections needed. No `app.json` config-plugin entry was needed for `react-native-svg` (confirmed no plugin-related install warning, unlike spec 14's `datetimepicker`). **Not visually confirmed on-device** — same standing gap as every other spec in this project (see Known Gaps); chart rendering itself (bar heights, donut segment proportions, month-label fit at phone width) is type/lint-verified only, not observed.
- **2026-07-26 (specs 18 + 19 planned)** — Per user request, wrote two new v2 specs — `18-spending-mileage-trend-charts.md` and `19-ai-integration.md` — as an implementation plan for the user to review before authorizing any code changes, mirroring two already-shipped web-client features (its specs 13 and 14) onto this app's own conventions. Explored this app's actual current code directly (not assumed from the web app) before writing either spec: `Spending.tsx`'s inline-per-tab pattern vs. `Mileage.tsx`'s separate-file-per-tab pattern (the two new specs deliberately follow each screen's own existing convention rather than forcing consistency between them), the flat `types/<domain>.types.ts` layout (vs. web's per-component `type/` subfolders), `MaterialCommunityIcons` as the only icon library, `react-native-paper` as the UI kit, and confirmed zero charting/SVG dependency existed anywhere in the app before this. Both specs' backend contracts (2 trend endpoints, 3 AI endpoints) were independently re-confirmed live via `curl` against the shared `bikelog_server` rather than trusted from the web app's own spec docs alone. User was asked to pick a React Native charting library via `AskUserQuestion` (no existing precedent in this app) and chose `react-native-gifted-charts` over `victory-native`/`react-native-chart-kit`, now recorded in spec 18. Added both specs to `context/specs/00-build-plan.md`'s index (continuing the existing `NN` numbering past the previously-highest `17`), and, while touching that table, also added a previously-missing row for spec 17 itself (a pre-existing doc-sync gap unrelated to this planning pass, fixed in passing since it was directly adjacent). Both new specs' own Verify sections were written with unchecked `[ ]` boxes (unlike every other spec in this project, which shows retroactively-filled `[x]` items), since this was explicitly a pre-implementation planning pass, not a build.
- **2026-07-26 (spec 17 implemented)** — Executed `context/specs/17-select-input-and-spending-date-filter-fixes.md` per user request, after the user reviewed the spec and explicitly declined further changes to it. Two user-reported on-device bugs, both real per the user's own live testing: `SelectPickerField` still showing blank after selecting a value despite spec 16's two prior fixes, and Spending's Month/Year tabs still being a plain free-typed text field. Rewrote `SelectPickerField.tsx` on `react-native-paper`'s `Menu`/`TouchableRipple` (not another native-`Picker` patch — reasoning: two targeted fixes already didn't resolve it on-device, so a third guess in the same unverifiable pattern wasn't worth it; the `Menu` rewrite was already anticipated as this project's own documented fallback plan). Removed `@react-native-picker/picker` from `package.json` (grep-confirmed fully unused afterward) and re-ran `yarn install`. Added shared `YearStepper`/`MonthStepper` components (extracted from `YearlyMileageTab.tsx`'s already-working stepper pattern), applied to Spending's Month/Year tabs (the reported bug) and, as the spec's own recommended same-pass addition, to Mileage's Monthly/Yearly tabs too (an identical bug found while investigating, plus removing `YearlyMileageTab.tsx`'s now-duplicated inline stepper code). `expo lint` and `npx tsc --noEmit` both pass clean. **Not visually confirmed on-device** — no simulator/device available in this environment, same standing caveat as every other spec; this is the single highest-priority thing to verify next, since this is `SelectPickerField`'s third fix attempt and the first two didn't fully work.
- **2026-07-23 (spec 16 implemented)** — Executed `context/specs/16-swipe-edit-and-picker-fixes.md` per user request, after the user reviewed the spec and gave the go-ahead. Two user-reported on-device bugs: swipe-revealed Edit button doing nothing across 5 card components, and `SelectPickerField` showing truncated/missing selected text. Root-caused both via source review (no device available): (1) all 5 cards' Edit/Delete wiring was structurally identical and correct — the one real lead was `TouchableOpacity` imported from `"react-native"` instead of `"react-native-gesture-handler"` for the swipe-revealed action buttons, a known cause of swallowed taps since those buttons sit outside `ReanimatedSwipeable`'s `GestureDetector`-wrapped content; swapped the import in all 5 files. Asked the user whether a direct (non-swipe) tap on the card also failed — answer was "haven't tried yet," so this remains a partially-open diagnostic, explicitly flagged rather than assumed resolved. (2) `SelectPickerField.tsx`'s hardcoded `height: 40` was clipping Android's native picker text; raised to `50`. (3) `MaintenanceLogFormModal.tsx` had a real load-order race — catalog options fetch only starts when the modal opens, the same render pass that sets the prefill ID, so the `Picker` briefly/permanently had a `selectedValue` matching no `Picker.Item`; fixed by gating both pickers behind their options being loaded, showing `SectionLoading` until then. `expo lint`/`tsc --noEmit` both pass clean.
- **2026-07-23 (spec 15 implemented)** — Executed `context/specs/15-bike-accessory-price.md` per user request, after the user reviewed the spec and gave the go-ahead. Backend contract for the new optional `price` field was confirmed by reading `../bikelog_server`'s actual source (`bikeAccessory.interface.ts`/`.model.ts`/`.validation.ts`) directly rather than waiting on the user, since the backend hasn't been deployed yet and its source is the only reference that can't be stale. Added `price?: number` to all 3 bike-accessory types, a "Price (optional)" field to `BikeAccessoryFormModal.tsx` (client-side `> 0` validation mirroring the backend's `.positive()`, omitted from payload when blank), and conditional `৳{price.toFixed(2)}` display to `BikeAccessoryCard.tsx`. `expo lint`/`tsc --noEmit` both pass clean. **This has not been exercised against a live backend** — the currently-deployed `bikelog_server` doesn't have this field yet; verify the deployment actually has it before relying on this in real use.
- **2026-07-23 (spec 14 implemented)** — Executed `context/specs/14-date-picker.md` per user request, after the user reviewed the spec and gave the go-ahead. Installed `@react-native-community/datetimepicker@8.4.4`. Created `components/main/shared/DatePickerField.tsx` and exported it from the shared barrel. Replaced all 5 date fields across 4 forms (`BikeFormModal.purchaseDate`, `FuelLogFormModal.date`, `BikeIssueFormModal.dateReported`, `MaintenanceLogFormModal.serviceDate`/`nextDueDate`) with `DatePickerField`, removing `BikeFormModal.tsx`'s now-unneeded `DATE_REGEX` validation. `purchaseDate`/`date`/`dateReported`/`serviceDate` are capped at `maximumDate={new Date()}` (past-event fields); `nextDueDate` is left uncapped (records a future due date). `expo lint` and `tsc --noEmit` both pass clean. Not visually verified — no simulator/device available in this environment, same standing caveat as every other spec.
- **2026-07-22 (specs 09–13 review pass — one real bug found and fixed)** — Per user request, re-audited specs 09–13's implementations against the actual backend source (not just the spec files) after finding they were already marked complete. Specs 09, 11, 12, 13 were verified correct as-shipped — no code changes needed, only their Verify checklists were filled in with annotations (specs 11–13's checklists had never actually been checked off despite the specs being marked "Complete," unlike every other spec in this project; now consistent). **Spec 10 had a real, live bug**: both `RemindersBanner.tsx` and `MaintenanceLogCard.tsx` assumed `maintenanceType` arrives as a populated `{_id, name}` object. Confirmed via `bikelog_server/src/app/modules/maintenanceLog/maintenanceLog.service.ts` that neither `getRemindersFromDB` (uses `.lean()`, no `.populate()`) nor `getMaintenanceLogsFromDB` ever populates `maintenanceType` — it's always a bare ObjectId string, exactly as spec 10's own Context section already said ("Known backend quirk... resolve client-side"), but the implementation had drifted from its own spec. Effect before the fix: every reminder card showed a blank type name, and every maintenance log card showed the generic fallback "Maintenance" instead of the real type — for every single log, unconditionally, not an edge case. Fixed by changing `TReminder.maintenanceType` to `string` and threading a `maintenanceTypes: TMaintenanceType[]` prop (fetched once in `MaintenanceLog.tsx`, shared with both children) into both components to resolve the ObjectId client-side, matching spec 10's own Design pseudo-code for `RemindersBanner`, which had the correct logic all along. **Also discovered**: the already-shipped web client's `RemindersBanner.tsx` has the identical bug — not fixed there per the cross-project rule (separate, independently-tracked project); flagged in Known Gaps instead. `expo lint` and `tsc --noEmit` pass clean after the fix.
- **2026-07-22 (specs 11–13 implemented)** — Executed `context/specs/11-spending-summary.md`, `12-bike-issues.md`, and `13-bike-accessories.md`. All three specs built together since they're independent. **18 new files created**, 0 existing files modified (all routes were one-liner additions, the hub tiles already existed). Created `types/spending.types.ts`, `types/bike-issue.types.ts`, `types/bike-accessory.types.ts`. Created `components/main/Spending/` (Spending.tsx with 3 inline sub-tabs + SpendingSummaryView.tsx), `components/main/BikeIssue/` (BikeIssue.tsx + BikeIssueCard.tsx + BikeIssueFormModal.tsx), `components/main/BikeAccessory/` (BikeAccessory.tsx + BikeAccessoryCard.tsx + BikeAccessoryFormModal.tsx). Created route wrappers: `app/bikes/[bikeId]/spending.tsx`, `issues.tsx`, `accessories.tsx`. Corrections made vs. spec samples: (1) Spending — used `cat.total` not `cat.amount` (backend interface confirmed via `bikelog_server/src/app/modules/spending/spending.interface.ts`); (2) Issues — omitted `notes` field entirely (backend validation has no such field, confirmed via `bikeIssue.validation.ts`); used `dateReported` not `reportedDate`; used `_id` not `id`; (3) Accessories — used `SelectPickerField` from spec 02 as its first real usage, no visual issues detectable in code-only verification, but will need device confirmation; used `_id` not `id`. All three used envelope access pattern (`data?.data`), `ReanimatedSwipeable`, `RefreshControl` as a prop, borderless-underline TextInput pattern, and existing shared components (`StatusBadge` with correct color tables, `SelectPickerField`, `EmptyState`, `SectionLoading`, `confirmDelete`). `expo lint` and `tsc --noEmit` both pass clean.

- **2026-07-22 (specs 03–04 implemented)** — Executed `context/specs/03-login.md` and `04-register.md` together (see "Completed" above). Both `app/auth.tsx` and `app/register.tsx` **already existed** (kept "as-is" by spec 01, since they already called the right endpoints) with a full implementation inlined directly in the route file — violating `code-standards.md`'s thin-wrapper route convention. Executed both specs' "Create" actions as "extract into a `components/main/Auth/*Form.tsx` + thin wrapper," matching the established convention, rather than literal from-scratch creation. Found and fixed a **real, previously-shipped bug** in the process: the old inline `auth.tsx` built the post-login user object from `result?.data`, which is always `null` on `/auth/login` (confirmed via `dummy-data.md` and the Postman collection's own test script, which only ever reads `body.token`) — so `handleSetUser` was always being called with an all-`undefined` object. It "worked" only because `AuthGuard` checks truthiness of `user`, not its contents. Fixed by decoding the JWT client-side instead (`jwt-decode`, previously an unused dependency) into `{userId, userEmail}`, matching the real payload shape signed in `bikelog_server/src/app/modules/user/user.services.ts` (`{userId, userEmail, userRole}` — no `name` claim). This also meant loosening `types/global.types.ts`'s `IUser.name` from required to optional, since populating it would require a `GET /auth/me` round-trip the project's docs explicitly say not to build into this flow. Also caught: spec 03's own Design sample has the identical `data`-based bug (and calls a nonexistent `useUser()` hook — the real one is `useUserContext()`; and uses a generic type argument on `usePost()`, which isn't a generic hook); both spec samples dropped `KeyboardAwareScrollView` despite `architecture.md`'s stack table mandating it on every form screen; both used Paper's `mode="flat"` + `COLORS.card` background instead of `ui-context.md`'s already-documented, already-verified borderless-underline `TextInput` convention. All of these were corrected in the implementation and noted in each spec's Verify section. The old inline versions also used hardcoded hex colors (`#f3f4f6`, `#d1d5db`, `"blue"`) instead of `COLORS`, fixed in the rebuild.
- **2026-07-22 (specs 09–10 implemented)** — Executed `context/specs/09-maintenance-catalog-settings.md` and `10-maintenance-logs-reminders.md` together. Created `types/catalog.types.ts` (`TMaintenanceType`, `TEngineOilType`, with payload types). Created `components/main/SettingsCatalog/SettingsCatalog.tsx` — two expandable sections with inline create forms + list display (borderless-underline TextInput pattern, `SelectPickerField` not needed for these plain text form fields). **Modified** `app/(tabs)/settings.tsx` (replaced placeholder with one-liner wrapper). Created `types/maintenance-log.types.ts` (`TMaintenanceLog` with polymorphic `maintenanceType` + `oilType` fields, `TCreateMaintenanceLogPayload`, `TReminder`, `TMaintenanceLogsApiResponse`). Created `components/main/MaintenanceLog/` (4 files: `MaintenanceLog.tsx` with `RefreshControl` prop + skeleton/empty states; `MaintenanceLogCard.tsx` with `ReanimatedSwipeable` swipe-right=Edit/left-swipe=Delete + polymorphic type-name resolution; `MaintenanceLogFormModal.tsx` with `SelectPickerField` for maintenanceType + conditional oilType picker (shown only when "Engine Oil" selected), `partsReplaced` comma-split to `string[]`, `nextDueOdometer` never sent via type exclusion; `RemindersBanner.tsx` with horizontal scroll of colored overdue/upcoming cards). Created `app/bikes/[bikeId]/maintenance-logs.tsx` route wrapper. Found and fixed a `react-hooks/exhaustive-deps` lint warning in the form modal by wrapping `maintenanceTypes`/`oilTypes` derivations in `useMemo`. `expo lint` and `tsc --noEmit` both pass clean.
- **2026-07-22 (specs 07–08 implemented)** — Executed `context/specs/07-fuel-logs.md` and `08-mileage.md` together. Created `types/fuel-log.types.ts` and `types/mileage.types.ts` (types matched against the already-shipped web client's type definitions). Created `components/main/FuelLog/` (3 files: `FuelLog.tsx` with pagination + `RefreshControl` as prop + skeleton/empty states; `FuelLogCard.tsx` with `ReanimatedSwipeable` swipe-right=Edit/`renderLeftActions`/green + swipe-left=Delete/`renderRightActions`/red + client-side `totalCost` computation; `FuelLogFormModal.tsx` with borderless-underline `TextInput` pattern, pill toggle for `isFullTank`, 409-error handling, never-send-`totalCost` via type structure). Created `app/bikes/[bikeId]/fuel-logs.tsx` route wrapper. Created `components/main/Mileage/` (5 files: `Mileage.tsx` 4-tab switcher with pill tabs; `MileageHistoryTab.tsx` fetching exact records + approximate summary with null-safe rendering; `MonthlyMileageTab.tsx` with month text input + client-side avg; `YearlyMileageTab.tsx` with prev/next year chevron selector + per-month card grid; `LifetimeMileageTab.tsx` with all-time totals card). Created `app/bikes/[bikeId]/mileage.tsx` route wrapper. Both screens are now reachable from the `BikeDetailPage` tiles (which already navigate to these segments). `expo lint` and `tsc --noEmit` both pass clean.
- **2026-07-22 (specs 05–06 implemented)** — Executed `context/specs/05-dashboard-bike-list.md` and `06-bike-hub.md` together (see "Completed" above for the full change list). Cross-checked spec 05's Context claims against the actual backend source (`bikelog_server/src/app/modules/bike/bike.service.ts`, `bike.model.ts`) and the already-shipped, already-verified web client, and found several inaccuracies shared by both specs (since 06 copied its assumptions from 05): (1) `GET /bikes` returns a plain array under `data`, not `{result, meta}` — there's no pagination on this endpoint at all; (2) the bike id field is `_id`, not `id`; (3) `useFetchData`'s resolved value needs `.data` drilling to reach the actual payload (confirmed via the web client's own `BikeDetailPage.tsx`: `const bike = data?.data;`) — both specs' pseudo-code treated the resolved value as the payload directly. Also fixed: spec 05's `RefreshControl` sample wrapped it around `ScrollView` as a JSX child, which isn't how RN's `RefreshControl` works (it's a prop on `ScrollView`, not a container); swipe-action direction was backwards vs. `ui-context.md`'s documented convention (`renderLeftActions`/`renderRightActions` were swapped relative to what "left-swipe reveals delete" actually means in `react-native-gesture-handler`'s coordinate system); used `react-native-gesture-handler/ReanimatedSwipeable` instead of the deprecated `Animated.Value`-based `Swipeable` both specs implicitly assumed. Placed `BikeFormModal.tsx` directly in `components/main/Bike/` (not `Dashboard/`) since both specs share it. Added `app/bikes/_layout.tsx` (`AuthGuard`-wrapped `<Slot />`) — required by `architecture.md`'s System Boundaries section but absent from both specs' file tables, since the `bikes/` stack sits outside `(tabs)` and isn't covered by that layout's guard. Resolved the "tab bar vs. header icon" open question per spec 05's own explicit Design table (2-tab bar). Hit and resolved a stale-cache issue: `.expo/types/router.d.ts` (gitignored, regenerates on `expo start`) still listed pre-spec-01 expense-tracker routes and had no knowledge of any route added this session, causing `tsc` to reject a legitimate typed-routes navigation call — removed the stale file (confirmed `tsc` passes clean without it, since `expo-router` falls back to a permissive base type) rather than working around a phantom error.

## Known Gaps / Open Questions

- **Nothing in this app has been exercised against a live `bikelog_server` instance, ever, for any spec.** No backend was running in any session so far, and no simulator/device/emulator is available in this environment (no `adb`, no `xcrun`). All 13 specs are code/type/lint-verified against the documented contract (`dummy-data.md`, the Postman collection, and — increasingly, as of the specs 05+ and the 09–13 review pass — the backend's actual source read directly) and cross-checked against the already-shipped web client, but no real request/response round-trip or on-screen render has been observed firsthand for anything in this app. This is the single highest-priority thing to do once a device/simulator + running backend are available — start with swipe gestures, modal layout, tab bar icons, the borderless-underline `TextInput` styling, and the reminders/maintenance-log screens specifically (the one place a real bug was already found by static review; there could be others static review can't catch).
- **The already-shipped web client (`bikelog_client-web-/`) has the same reminders-banner bug this session fixed in the mobile app** — `components/(main)/MaintenanceLog/RemindersBanner.tsx` reads `r.maintenanceType.name` assuming a populated object, but the backend never populates it (confirmed directly in `bikelog_server/src/app/modules/maintenanceLog/maintenanceLog.service.ts`). This is a separate, independently-tracked project — per the cross-project rule, this was not fixed here, only flagged. Worth surfacing to whoever maintains that project; the fix is the same shape as the one applied here (resolve the ObjectId against the fetched maintenance-types list instead of assuming population).
- **`.expo/types/router.d.ts` needs a real `expo start` run to regenerate accurately.** It was deleted (not hand-edited) after specs 05/06 found it stale; it's gitignored and will repopulate correctly the next time the dev server actually runs. Until then, `tsc` runs without it (confirmed clean) rather than against a misleading snapshot.
- **`SelectPickerField`'s `Menu`-based rewrite (spec 17) is not yet confirmed on-device.** This is the third fix attempt for the same blank-value symptom — spec 16's two narrower patches (picker height, prefill-race guard) were both confirmed present in code but the user still saw the bug live, so this rewrite sidesteps the native-`Picker` rendering path entirely rather than guessing at a third native-widget-specific cause. Needs real-device confirmation for both a static-options usage (`BikeAccessoryFormModal`) and a catalog-fetched usage (`MaintenanceLogFormModal`), in both create and edit (prefill) flows, before considering this bug actually closed.
- **Spending's/Mileage's new `MonthStepper`/`YearStepper` controls (spec 17) are code-verified only.** The chevron-stepper UI itself is a proven pattern (lifted from `YearlyMileageTab.tsx`'s already-shipped year stepper), but has never been visually confirmed for the new `MonthStepper` specifically, nor for either stepper's placement inside `Spending.tsx`'s Month/Year tabs — no simulator/device in this environment.
- **Final `COLORS` values are a first pass, not a design decision** — spec 01 picked concrete hex values (`primary: "#2196F3"` etc.) to unblock every later spec that needs `COLORS` to exist; revisit the actual palette whenever real screens make it visually evaluable, per spec 01's own note that this is "purely aesthetic, not urgent."
- **`DatePickerField`'s native picker rendering is unconfirmed** (spec 14) — same category of gap as the Picker/Menu decision above: `@react-native-community/datetimepicker` is a real native module (Android dialog / iOS spinner), and neither its visual fit against the app's borderless-underline field convention nor its actual on-device behavior (dialog dismiss timing, spinner interaction) has ever been exercised — no simulator/device in this environment for any spec, including this one.
- **Spec 16's swipe-Edit fix is a well-founded hypothesis, not a confirmed root cause.** The `TouchableOpacity`-from-`react-native-gesture-handler` swap is the standard fix for this exact class of symptom, but static review couldn't fully explain why Delete worked while Edit didn't (both used the identical touchable before the fix). The user has not yet confirmed whether tapping a card _directly_ (no swipe) ever failed too — if it did, the real bug is downstream of the tap (Modal/Portal rendering) and this fix alone won't resolve it. Ask the user to confirm this on their next device session; if Edit is still broken after this fix, that diagnostic is the next step, not another gesture-related tweak.
- **`SelectPickerField`'s height fix (40→50, spec 16) and `MaintenanceLogFormModal`'s prefill-race fix are code-verified only** — no simulator/device available in this environment to confirm the text no longer clips or that the prefilled catalog picker now shows correctly once loaded.
- **Bike accessory `price` (spec 15) has never been exercised against a live backend** — implemented purely by reading `../bikelog_server`'s source, since that backend hasn't been deployed with the field yet. Submitting a price will 400 against whatever backend is actually live until the deployment happens. Confirm the deployed backend accepts/returns `price` before relying on this in real use; also note (per spec 15's own scope) that accessory `price` is deliberately **not** wired into the spending-summary totals on either side.
- **Spec 18's trend charts (`react-native-gifted-charts`) are code/type-verified only, never rendered on a real device or simulator** — same standing gap as everything else in this app, but with extra weight here since this is the app's first-ever charting dependency: bar-height scaling, donut-segment proportions, and month-label fit at phone width are all unconfirmed. `react-native-svg` was deliberately pinned to `15.12.1` (Expo SDK 54's expected version) rather than the `^15.15.5` that `yarn add` initially installed — worth double-checking this pin still holds after any future `expo upgrade`.
- **Spec 20's image-upload UI (`ImagePickerField`/`MultiImagePickerField`) is code/type-verified only, never exercised on a real device or against a live backend** — same standing gap as everything else in this app, but with extra weight here since this is the app's first-ever camera/photo-library permission: the actual native permission prompts, action-sheet UX, and thumbnail rendering (tile sizing, `X`-overlay hit target, spinner overlay) are all unconfirmed. `bikelog_app/CLAUDE.md`'s own "Server state" line ("no PUT hook — `bikelog_server` has no PUT routes") is now stale in the same way `architecture.md`'s was (already corrected there) — not edited in `CLAUDE.md` itself since that was out of this spec's explicit scope (only `architecture.md`'s claim was listed in the spec's own Implementation checklist), but flagged here for whoever next touches that file.
- **Spec 19's chat screen and insight cards are otherwise still code/type-verified only** — one real on-device bug has now been found and fixed here (see Recent Activity: the chat input/send button were hidden behind the keyboard, fixed with `KeyboardStickyView`), but everything else about the screen (bubble layout, `IconButton`/`TextInput` sizing on a real phone, the actual send/thinking/reply flow) is still unconfirmed on-device. The chat round-trip against a live `bikelog_server` was exercised via `curl` (not from this app's UI) during the prior planning session.
- **Spec 21's image viewer (`ImageViewerModal`, plus the `ImagePickerField`/`MultiImagePickerField` tap-target rewiring) is code/type-verified only, never exercised on a real device or simulator** — same standing gap as everything else in this app: the actual full-screen modal layout, tap-outside-to-close behavior, swipe-paging feel, chevron hit targets, and the new pencil-badge's placement/hit target next to the existing delete `X` on `ImagePickerField`'s 64×64 tile are all unconfirmed. This spec's 4 originally-open questions (tap-target conflict, nav mechanism, pinch-zoom scope, wrap-around) were decided rather than asked, per explicit instruction — see Recent Activity for the exact choices; none of the 4 have been revisited or challenged by the user yet.
- **Spec 22's bike documents feature (`MultiFilePickerField`, `BikeDocument`/`BikeDocumentCard`/`BikeDocumentFormModal`) is code/type-verified only, never exercised on a real device or against the live backend** — same standing gap as everything else in this app, with extra weight here since this is the app's first-ever `expo-document-picker` (PDF) permission: the 4-option action sheet, mixed image/PDF tile rendering (thumbnail vs. file-icon-plus-label), the PDF `Linking.openURL` hand-off to the OS's own viewer/browser, the expiry-badge's 3-way color/label branch, and the 10-file-per-request cap's info toast are all unconfirmed on a real device. The backend side (`bikelog_server` spec 19) was itself live-verified via curl on the server project's own session, but that round-trip has not been re-exercised from this app's UI.

## Next Up

All 22 specs (the full v1 build, both halves of the v2 planning pass — trend charts and AI integration — image uploads, the full-screen image viewer, and now bike documents) are complete, matching the backend's current scope through its own spec 19. **No further spec implementation work is currently planned.** Visual verification on a device/simulator against a live `bikelog_server` instance remains the single highest-priority gap across the whole app (see Known Gaps) — no spec's UI, including specs 18/19/20/21/22's new screens, has ever been rendered outside of type/lint checking; this is the natural next session's focus once a device becomes available. A source-level review pass of specs 01–08 (mirroring the one already done for 09–13 on 2026-07-22, which found and fixed one real bug) also remains undone if further static-review time is available before a device shows up.
